<Window x:Class="OptimalFuzzyPartition.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OptimalFuzzyPartition"
        xmlns:u="clr-namespace:OptimalFuzzyPartition.View"
        mc:Ignorable="d"
        Title="Оптимальне нечітке розбиття множин - побудова нечіткої діаграми Вороного" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <DockPanel>
            <ScrollViewer>
                <StackPanel>

                    <Label Content="Часу пройшло: "></Label>
                    <Label Name="LabelTimePassedValue" Content="- "></Label>

                    <Button Margin="5" Name="ButtonOpenLogs" Content="Відкрити віконце логів"></Button>

                    <!--<Label Content="{Binding StringFormat=Ітерацій пройшло: {0}, Path=}"></Label>-->

                    <DataGrid>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Номер центру" IsReadOnly="True"></DataGridTextColumn>
                            <DataGridTextColumn Header="X" IsReadOnly="True"></DataGridTextColumn>
                            <DataGridTextColumn Header="Y" IsReadOnly="True"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                </StackPanel>
            </ScrollViewer>

            <u:UnityWindowHost></u:UnityWindowHost>
        </DockPanel>
    </Grid>
</Window>
